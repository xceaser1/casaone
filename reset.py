from app import app; from models.db import db; from models.auth import User, Role; app.app_context().push(); r=Role.query.filter_by(nom="ADMIN").first(); u=User.query.filter_by(username="admin").first() or User(username="admin", nom_complet="Admin", actif=True); u.role_id=r.id; u.actif=True; u.set_password("admin123"); db.session.add(u); db.session.commit(); print("OK : admin / admin123")
