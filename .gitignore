venv/
__pycache__/
*.pyc
database/*.db
database/*.db.bak-*
database/.secret_key
# Fichiers deposes par les utilisateurs : classeurs importes, photos et PDF
# joints aux demandes et aux mouvements. Le dossier est recree au demarrage.
# Ne couvrir que *.xlsx laissait passer les sous-dossiers, et une photo de
# chantier serait entree dans l'historique.
uploads/

# Fichiers d IDE
.idea/
.vscode/

# Projet Android : artefacts de build et secrets de signature
android/build/
android/app/build/
android/.gradle/
android/local.properties
android/keystore.properties
android/*.jks
android/*.keystore

# APK publie : binaire volumineux, distribue mais pas versionne
dist/
